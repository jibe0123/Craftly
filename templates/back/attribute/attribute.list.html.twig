{% extends 'back/base.html.twig' %}

{% block body %}
    <p>Liste des attributs:</p>
    <ul>
        {% for a in attr %}
            <p>{{a.name}}         &nbsp   &nbsp      {{a.unit}}</p>
        {% endfor %}
        <a href="{{ path('back_new_attribute')}}">{{ 'Créer'}}</a>
    </ul>
{% endblock %}
